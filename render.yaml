services:
  # ── Web Service (Laravel App) ──────────────────────────────────────────────
  - type: web
    name: ticketeria-app
    runtime: docker
    dockerfilePath: ./Dockerfile
    # Root directory: leave blank (app is at repo root, not in /backend)
    plan: free
    healthCheckPath: /
    envVars:
      - key: APP_NAME
        value: "Sistema de Tickets"
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: APP_URL
        fromService:
          type: web
          name: ticketeria-app
          property: host
      - key: APP_KEY
        generateValue: true
      - key: DB_CONNECTION
        value: pgsql
      - key: DATABASE_URL
        fromDatabase:
          name: ticketeria-db
          property: connectionString
      - key: DB_SSLMODE
        value: require
      - key: CACHE_DRIVER
        value: file
      - key: SESSION_DRIVER
        value: file
      - key: QUEUE_CONNECTION
        value: sync
      - key: LOG_CHANNEL
        value: stderr
      - key: LOG_LEVEL
        value: debug
      - key: APP_DEBUG
        value: "true"
      - key: DB_SSLMODE
        value: require
      # Mail — set these manually in the Render dashboard
      - key: MAIL_MAILER
        value: smtp
      - key: MAIL_HOST
        value: smtp.gmail.com
      - key: MAIL_PORT
        value: "587"
      - key: MAIL_ENCRYPTION
        value: tls
      - key: MAIL_FROM_NAME
        value: "Sistema de Tickets"
      # MAIL_USERNAME, MAIL_PASSWORD, MAIL_FROM_ADDRESS → add in dashboard

  # ── PostgreSQL Database ────────────────────────────────────────────────────
databases:
  - name: ticketeria-db
    databaseName: sistema_tickets
    plan: free
